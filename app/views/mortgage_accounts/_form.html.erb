<%= form_with(model: mortgage_account, local: true) do |form| %>
  <% if mortgage_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mortgage_account.errors.count, "error") %> prohibited this mortgage_account from being saved:</h2>

      <ul>
      <% mortgage_account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :balance %>
    <%= form.text_field :balance %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
