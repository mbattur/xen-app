<%= form_with(model: stack_account, local: true) do |form| %>
  <% if stack_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stack_account.errors.count, "error") %> prohibited this stack_account from being saved:</h2>

      <ul>
      <% stack_account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :balance %>
    <%= form.text_field :balance %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
