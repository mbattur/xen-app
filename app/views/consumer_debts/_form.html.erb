<%= form_with(model: consumer_debt, local: true) do |form| %>
  <% if consumer_debt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(consumer_debt.errors.count, "error") %> prohibited this consumer_debt from being saved:</h2>

      <ul>
      <% consumer_debt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :balance %>
    <%= form.text_field :balance %>
  </div>

  <div class="field">
    <%= form.label :monthly_minimum %>
    <%= form.text_field :monthly_minimum %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
